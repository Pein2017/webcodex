## 1. Gateway ACK package

- [x] 1.1 Reproduce and fix Plugin wrapper ACK handling through the public invocation path; test valid ACK observation, repeated ACK, omitted ACK redelivery, malformed and unauthorized/wrong-Session cases, and provider argument isolation.

## 2. Skill and discovery package

- [x] 2.1 Add guarded executable Skill continuations for Runner and project sources; replay next-page calls, verify EOF and stale-definition rejection with focused RED/GREEN tests.
- [x] 2.2 Add bounded safe logical identity/scope to invalid Skill diagnostics; verify missing-definition identification and path privacy.
- [x] 2.3 Extend existing exploration discovery and Web guide with narrow relationship queries and freshness-aware fallback; verify manifest selection and existing payload bounds.

## 3. Lead integration

- [x] 3.1 Reproduce structured LSP error loss and preserve classification through Session recording/handoff; verify specific code, generic fallback and bounded/redacted evidence with focused tests.
- [x] 3.2 Review exact combined diff and run affected tests, formatting, strict OpenSpec validation, and Git whitespace/status checks; record evidence and remaining deployment boundary in acceptance.md.
